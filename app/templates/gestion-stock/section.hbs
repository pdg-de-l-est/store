{{page-title "Section"}}
<form {{action "multiUpdate" @model.section.products this._stock on="submit"}}>
  <div class="ui small action input" style="margin-left: 1rem">
    <div class="ui right pointing label">
      Ajout de stock pour plusieurs objets à la fois
    </div>
    <Input @value={{this._stock}} type="number" required="required" placeholder="Ajouter"/>
    <button class="ui icon button" type="submit">
      <i class="plus icon"></i>
    </button>
  </div>
</form>
<br>
<div class="ui six cards">
  {{#each @model.section.products as |product|}}
    <div class="card" style="float: left">
      <div class="content">
        {{product.name}}
      </div>
      <div class="image" style="height: 200px;margin: auto;">
        <img src="/images/{{product.image}}.jpg" class="card-img-top" alt="" />
      </div>
      <div class="content">
        <i class="box icon"></i>
        {{product.stock}}
        <span class="right floated">
          <div class="ui checkbox">
            <Input type="checkbox" name="example" {{action "checked" product on="change"}}/>
            <label></label>
          </div>
        </span>
      </div>
      <div class="content">
        <span class="right floated">
          <form {{action "update" product stock on="submit"}}>
          <div class="ui mini action input">
            <Input @value={{product._stock}} required="required" type="number" placeholder={{product.stock}}/>
            <button type="submit" class="ui vertical animated button" tabindex="0">
              <div class="hidden content">Ajouter</div>
              <div class="visible content">
                <i class="plus icon"></i>
              </div>
            </button>
          </div>
          </form>
        </span>
      </div>
    </div>
  {{/each}}
</div>
{{outlet}}